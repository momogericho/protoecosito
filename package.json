{
  "name": "sitopol-tests",
  "private": true,
  "devDependencies": {
    "lighthouse": "^12.8.2",
    "puppeteer": "^22.5.0"
  },
  "scripts": {
    "test:offline": "node tests/offline-cache.test.js",
    "lighthouse": "lighthouse http://localhost:8080 --quiet --chrome-flags='--headless --no-sandbox' --only-categories=performance,accessibility,best-practices,pwa"
  }
}